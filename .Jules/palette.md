# Palette's Journal

## 2024-05-22 - Starting the Journey
**Learning:** Accessibility is a continuous process. Even small components like a graph node need keyboard navigation to be truly inclusive.
**Action:** Always check for `tabIndex`, `role`, and `onKeyDown` handlers on interactive elements that are not native buttons or links.
